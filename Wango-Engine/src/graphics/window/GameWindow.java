package graphics.window;

public class GameWindow extends Window{
    //TODO read window properties from config file
    public GameWindow(String title, int sizeX, int sizeY) {
        super(title, sizeX, sizeY);
    }
}
